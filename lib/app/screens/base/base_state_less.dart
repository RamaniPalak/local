import 'package:flutter/material.dart';
import 'package:local/app/screens/base/base_state.dart';

abstract class BaseStateLess extends StatelessWidget with BaseScreen {
  BaseStateLess({Key? key}) : super(key: key);

}